using UnityEngine;

public class herenciadevelocidad : MonoBehaviour
{
    public int speedx = 5;
    public int speedy = 5;
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
